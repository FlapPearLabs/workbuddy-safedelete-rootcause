# Allow-rule experiment: NOT EXECUTED in this round
#
# The full procedure for running the allow-rule empirical test inside a real
# WorkBuddy session is in:
#   ../NEXT-WORKBUDDY-GIT-EXPERIMENT.md
#
# This file remains as a high-level reference. The full procedure handles:
#   - Phase 1: REAL WORKBUDDY BASELINE (no rule changes, kernel filter active)
#   - Phase 2: WorkBuddy-native rule A/B (add narrow inherit_user rule, restart, re-run)
#   - Phase 3: Optional ETW / ProcMon capture of the kernel-filter denials
#
# The full procedure includes:
#   * Sanity checks (WorkBuddy install present, shim available)
#   * Per-step structured records (WORKTREE_CHECK_* markers)
#   * Standardized outcome format (WORKTREE_LOSS_REPRODUCED, etc.)
#   * tsbx_rules.json backup + atomic edit + restore
#   * Hard rules: no force-push, no reset --hard, no clean -fd, no production-repo touch
#
# What was prepared (see ../bin/ for the scripts):
# 1. Original tsbx_rules.json backed up at:
#      ../report/tsbx_rules.original.json
#    sha256: 30A07E5FB92AD06D7EFD3A0DA7F1AA796CBDF3C3517EF1D70C8FA1E658B9A45A
# 2. The proposed rule (added to file_rules_user):
#      { "path": "<WORKSPACE>\\**", "type": "inherit_user" }
# 3. The Git probe is generated at runtime by bin/build-git-probe.ps1
#    (60 tracked files on master, 48 modified / 5 deleted / 16 added / 4 renamed on feature).
#
# Why not auto-executed:
#   Applying the proposed narrow rule to the live tsbx_rules.json would require
#   either a WorkBuddy restart (unsafe while the user is in an active WorkBuddy
#   session) or a hot-reload mechanism that we could not confirm from outside.
#   The procedure in NEXT-WORKBUDDY-GIT-EXPERIMENT.md pauses for explicit user
#   consent before modifying the live tsbx_rules.json.
#
# Expected outcomes:
#   Phase 1 (no rule change):
#     WORKTREE_LOSS_REPRODUCED: <YES | NO_IN_10_CYCLES>
#   Phase 2 (with inherit_user rule covering only the lab root):
#     WORKTREE_LOSS_REPRODUCED: <YES | NO_IN_10_CYCLES>
#
#   If Phase 1 = YES and Phase 2 = NO:
#     SANDBOX_POLICY_CAUSE_CONFIRMED
#   If Phase 3 (ETW/ProcMon) also captures the kernel-filter denials:
#     COMPONENT_LEVEL_CAUSE_CONFIRMED
#
#   If Phase 1 = NO_IN_10_CYCLES:
#     The investigation stays at HIGH_CONFIDENCE_INFERENCE for Issue B's
#     component-level cause, and the bug report's "Severity" and "Issue B"
#     sections should be downgraded accordingly.
