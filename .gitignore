# =============================================================================
# Disposable lab probe fixtures and runtime state from this investigation.
# =============================================================================
# These are intentionally NOT committed to the public repro bundle:
#   * They are runtime-generated by bin/build-fixture.ps1 / bin/build-git-probe.ps1
#   * They live under fixtures/ and work/ which are gitignored
#   * They were captured only as proof that the probes were run in this lab
# =============================================================================

# Runtime-generated probe fixtures (built fresh by bin/build-*.ps1)
fixtures/
work/

# Legacy probe directories that the old script layout left in the repo root.
# Kept here so that if a stale git-probe-allow-rule/ exists locally it does
# not get accidentally re-committed.
git-probe-allow-rule/
git-probe-normal/
git-probe-workbuddy/
node-delete-probe/

# npm-probe runtime artifacts (rebuilt by repro-npm-ci.ps1)
npm-probe/node_modules/
npm-probe/_manifest_phase1.txt
npm-probe/_manifest_phase2.txt
npm-probe/_manifest_*.txt
npm-probe/_node_modules.sha256

# Per-run shim state and report temp dirs
workbuddy-rootcause-control/
